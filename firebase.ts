/**
 * Deccan Serai - Local Storage Implementation
 * Firebase has been replaced with a high-performance local simulation 
 * to ensure maximum stability in the web preview environment.
 */
export const db = {
  // Safe mock for existing references
  isMock: true
};